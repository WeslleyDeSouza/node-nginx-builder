{
  "name": "nginx-builder",
  "version": "0.0.3",
  "description": "",
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "build:start": "npm run build && node test.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "wes",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.7.14"
  },
  "dependencies": {
    "nginx-format": "^0.1.7"
  }
}
